# 统一换行符，避免不同平台 clone 后行为不一致
* text=auto eol=lf

# 批处理必须是 CRLF，否则某些 cmd 版本解析会出问题
*.bat text eol=crlf

# 明确指定，避免被 auto 猜错
*.py   text eol=lf
*.md   text eol=lf
*.txt  text eol=lf

# 二进制文件不要动
*.png  binary
*.jpg  binary
*.zip  binary
*.prt  binary
*.rvt  binary
